# Handover — 2026-04-23

## Situation

**Project:** Vibeclubs (`C:\Users\frank\vibeclubs.ai`). Public MIT repo at
`github.com/frankxai/vibeclubs`. Sprint 1 runs Apr 17 – May 14 2026.

**State at session end:** Main branch at `0753642`, clean. Previous session
(Apr 22) landed Phase 2/3 3D scenes + design-core + vibe mechanics +
autonomous content/clubs directory. Today's session built the **ops
harness**: scripts, CI workflows, 8 Claude Code subagents, 4 new slash
commands, ops docs.

**Goal of next session:**
1. Provision Supabase (creds → `apps/web/.env.local`) to unblock `/start`
   hosted path. Autonomous path already works without it.
2. Run `/vibe-check` end-to-end in CI (first PR to trigger the new
   workflows).
3. Extension overlay rebuild to add ship + dance phase UI (vibe mechanics
   #3 and #6).

## What's Done (this session, 4 pushed commits)

**Ops scripts (`scripts/`)** — `a67b12e`
- `scripts/voice-audit.mjs` — case-sensitive forbidden-word scan with
  identifier-boundary lookaround, denial detection across current line +
  4 preceding, line-level `voice-audit: allow` escape hatch, `--json` flag.
- `scripts/validate-clubs.mjs` — frontmatter schema validator for
  `content/clubs/*.md`. Strict enums + slug regex + min body length.
- `scripts/new-club.mjs` — interactive CLI scaffold. Prompts through entire
  schema with on-the-fly validation.
- `package.json` — `audit:voice`, `audit:clubs`, `audit:all`, `new:club`
  scripts.

**CI workflows (`.github/workflows/`)** — `61a46c4`
- `voice-audit.yml` — runs on every PR touching `apps/**`, `content/**`,
  `README.md`. < 5 min.
- `club-validate.yml` — runs on every PR touching `content/clubs/**`. < 3
  min.
- `release-packages.yml` — tag-triggered npm publish for `@frankx/design-core`
  and `@vibeclubs/*`. `workflow_dispatch` with dry-run default.
- `.github/ISSUE_TEMPLATE/club-submission.md` — structured flow for PR-
  based club listings.
- `.github/ISSUE_TEMPLATE/vibe-mechanic.md` — enforces the three principles
  + four failure modes before asking for review.
- `.github/pull_request_template.md` — rewritten with full pre-ship
  checklist, VISION.md tests, vetoes checklist, agent-dispatch list.

**Agents + commands (`.claude/`)** — `d12b5ab`
- Agents: `club-curator`, `voice-auditor`, `vibe-mechanic-designer`,
  `design-keeper`, `research-drafter`, `cohort-coordinator`,
  `ecosystem-planner`, `session-recapper`. Each with frontmatter (name,
  description, tools) and canonical specs.
- Commands: `/vibe-check`, `/club-add`, `/curate`, `/handover`.

**Docs + CLAUDE.md + voice fixes** — `0753642`
- `docs/ops/README.md` — one-screen ops harness doc with failure-mode
  runbook.
- `CLAUDE.md` — updated with ops section + "second file to read" pointer.
- `README.md` — "operator guide" → "dev guide" voice fix.
- `apps/web/app/playbook/playbook-data.ts` — voice fixes in GenCreator
  cross-post (community drop → crew drop, cohort members → past cohort
  grads).
- `apps/web/components/patterns/ship-drop.tsx` — JSDoc voice fix.

## What's Not Done

- **Supabase not provisioned.** Hosted `/start` flow still blocked. The
  autonomous `content/clubs/*.md` path works without it.
- **Extension overlay for ship + dance phases.** The pomodoro-sync
  package already broadcasts `ship` and `dance` phase events. The
  `apps/extension/contents/overlay.tsx` overlay hasn't been updated to
  render them. 2–4 hour sprint.
- **Real-time presence on `/club/[slug]`.** Channel exists (pomodoro
  broadcast); presence listener + avatar glow UI not wired. Half-day.
- **Suno cycle-aware prompt ramp.** Requires Suno API access + a one-line
  extension to `@vibeclubs/suno-bridge`.
- **First CI run of the new workflows.** Nothing's triggered them yet
  because no PR has opened since the push. Worth watching on the next PR.
- **npm publish of `@frankx/design-core` + `@vibeclubs/*`.** Release
  workflow exists but no tag yet. Recommend publishing design-core first
  since it's the dependency root.

## Critical Context

**Forbidden writes (still!):**
- Do NOT run `git push --force`, `git reset --hard`, or any destructive git op.
- Do NOT provision external services (Supabase / Vercel / Chrome Web Store
  / npm-registry tokens) — Frank owns those.
- Do NOT add new forbidden words to the voice audit without a matching
  update to `VISION.md §Voice` and `.claude/skills/voice/SKILL.md`. The
  3-file rule is non-negotiable.

**Voice audit is now strict.** The CI workflow will fail any PR that
introduces consumer-surface uses of the forbidden vocabulary. If you need
to use a forbidden word intentionally in meta-copy (e.g., "not a community"),
the audit's denial detection catches common patterns; for edge cases use
`<!-- voice-audit: allow -->` inline.

**Build skips TS errors.** `next.config.mjs` sets
`typescript.ignoreBuildErrors: true` to work around pre-existing Radix UI
+ React 19 type friction. `pnpm typecheck` still surfaces the residual
errors. Don't remove this gate until the Radix ecosystem catches up.

**The Supabase Database type is hand-written** in
`apps/web/lib/supabase/types.ts`. When Supabase is provisioned, regenerate
via `supabase gen types typescript` and replace. Until then, any schema
change requires updating the hand-written types.

**Design-core extraction is partial.** `@frankx/design-core` tokens.css +
motion.css + themes/vibeclubs.css are imported by `apps/web/app/globals.css`
but the site still keeps its own `@theme { }` block for Tailwind 4 utility
generation (literal values). Keep both in sync when adding tokens.

## Next Actions (ordered)

1. **Provision Supabase** (only Frank can do this). See
   `ENVIRONMENT.md §1`. After creds land, run `/migrate` + `/seed`.
2. **First CI smoke** — open a trivial PR (e.g., adding your name to
   `CODEOWNERS` or a new `content/clubs/<your-slug>.md`) to watch the new
   voice-audit + club-validate workflows run. Confirms they don't flake.
3. **Extension overlay rebuild** for ship + dance phases. Edit
   `apps/extension/contents/overlay.tsx`. Add two new overlay states keyed
   off the `ship` and `dance` events emitted by `@vibeclubs/pomodoro-sync`.
4. **Publish `@frankx/design-core` to npm.** `git tag @frankx/design-core@0.1.0`
   then push the tag. Release workflow handles the rest (dry-run by default;
   use `workflow_dispatch` with `dry-run: false` to publish live).
5. **Vercel import.** Import `frankxai/vibeclubs`, bind `vibeclubs.ai`,
   paste env vars. First canonical preview.
6. **Chrome Web Store $5 dev fee** — non-blocking; sideload unlisted first
   works now.

## Files to Read First

For a cold-starting Claude on Monday morning:

- `C:\Users\frank\vibeclubs.ai\VISION.md` — north star + voice system. Always first.
- `C:\Users\frank\vibeclubs.ai\docs\ops\README.md` — ops harness. Always second (new).
- `C:\Users\frank\vibeclubs.ai\CLAUDE.md` — dev guide. Updated today with ops pointer.
- `C:\Users\frank\vibeclubs.ai\docs\strategy\ecosystem-role.md` — how vibeclubs sits in the cross-brand constellation.
- `C:\Users\frank\vibeclubs.ai\docs\strategy\vibe-mechanics.md` — the six mechanics that make this actually vibe.
- `C:\Users\frank\vibeclubs.ai\docs\strategy\design-evolution.md` — motion + 3D + palette rules.
- `C:\Users\frank\vibeclubs.ai\docs\ops\HANDOVER-2026-04-21.md` — prior handover (repo bootstrap).
- `C:\Users\frank\vibeclubs.ai\.claude\agents\` — 8 specialized subagents.

## Repo Map (changed this session)

| Path | State |
|---|---|
| `scripts/` | **NEW.** 3 executable Node 20 scripts — voice-audit, validate-clubs, new-club |
| `.claude/agents/` | **NEW.** 8 specialized Claude Code subagents |
| `.claude/commands/vibe-check.md` | **NEW.** Full pre-flight runbook |
| `.claude/commands/club-add.md` | **NEW.** Interactive club scaffold |
| `.claude/commands/curate.md` | **NEW.** Dispatch club-curator on a listing |
| `.claude/commands/handover.md` | **NEW.** Dispatch session-recapper on end of session |
| `.github/workflows/voice-audit.yml` | **NEW.** CI voice-system gate |
| `.github/workflows/club-validate.yml` | **NEW.** CI frontmatter gate |
| `.github/workflows/release-packages.yml` | **NEW.** Tag-triggered npm publish |
| `.github/ISSUE_TEMPLATE/` | **NEW.** Club submission + vibe mechanic templates |
| `.github/pull_request_template.md` | **REWRITTEN.** Full pre-ship checklist |
| `docs/ops/README.md` | **NEW.** Ops harness discoverability doc |
| `CLAUDE.md` | **UPDATED.** New ops section + second-file-to-read pointer |
| `README.md` | Voice fix: operator → dev |
| `apps/web/app/playbook/playbook-data.ts` | Voice fixes in cross-post |
| `apps/web/components/patterns/ship-drop.tsx` | Voice fixes in JSDoc |
| `package.json` | New scripts: audit:voice, audit:clubs, audit:all, new:club |

## Relevant memory entries

All under `~/.claude/projects/C--Users-frank-vibeclubs-ai/memory/`:

- `MEMORY.md` — index
- `user_role.md` — Frank is solo, AI-augmented, 5-brand ecosystem
- `project_architecture.md` — ADR-002 active
- `project_sprint.md` — Sprint 1 Apr 17 – May 14
- `project_ecosystem_map.md` — cross-brand constellation (added Apr 22)
- `feedback_collaboration.md` — Frank wants proactive leadership
- `feedback_voice.md` — five-word voice system
- `reference_north_star.md` — read VISION.md first

Future sessions should read `MEMORY.md` first → follow pointers → then
`CLAUDE.md` → then `docs/ops/README.md`. The ops doc is the short-form
map of the harness Frank ships with today.

## Change log vs earlier version

This is the first version of the 2026-04-23 handover.
