# Homepage Scene Brief

## Metaphor

A late-night studio instrument for collective focus. The interface behaves like a calm control surface, not a social feed.

## Primary asset

Tier C code-native product proof. It calls the actual monorepo timer state machine and recap-card renderer. This is stronger evidence than a generated hero image, so image generation is deliberately not used.

## Motion beat

- Entry: static hierarchy first; subtle `proof-breathe` atmosphere behind it.
- Action: state rail advances only as the visitor operates the proof.
- Focus: countdown communicates real elapsed state.
- Resolution: exact recap and generated SVG appear after a ship note.
- Reduced motion: decorative glow freezes; the timer remains numerically functional.

## Future media

After the hosted loop is stable, capture Tier A footage of two real browsers synchronizing and producing a recap. A 10–15 second silent product film may then replace supporting copy on wide screens. Do not commission 3D until a distinct, useful brand metaphor exists.
